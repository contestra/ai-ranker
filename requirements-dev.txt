# requirements-dev.txt
# Minimal dev dependencies for the Prompter V7 local stack (API, tests, migrations, metrics)

# --- Web/API ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# --- ORM & DB ---
SQLAlchemy>=2.0.30
alembic>=1.13.0
psycopg2-binary>=2.9.0   # Postgres driver for Alembic/tests; not needed for pure SQLite dev

# --- Validation ---
pydantic>=2.5,<3.0

# --- Metrics ---
prometheus-client>=0.20.0

# --- Redis (optional; used by ensure-version idempotency) ---
redis>=5.0.0

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0             # FastAPI TestClient dependency

# --- Misc ---
python-dotenv>=1.0.0      # Optional: env file loading
